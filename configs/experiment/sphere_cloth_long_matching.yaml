# @package _global_

defaults:
  - /trainer: default
  - /optimizer: adam
  - /scheduler: step
  - /logger: wandb
  - /plot: wandb
  - /model: pde_matching_igns
  - /callbacks: default
  - /cluster: local
  - /env: sphere_cloth
  - /dataset: sphere_cloth_long
  - override /hydra/job_logging: colorlog
  - override /hydra/hydra_logging: colorlog
  - _self_

# Global configurations
experiment_name: SphereCloth
seed: 42
lr: 5e-4
epochs: 5000
batch_size: 4
num_workers: 4
scheduler: null
debug: False
training_noise: False
training_noise_std: 1e-2
val_per_steps: 10
rollout_train: False
rollout_val: False

trainer:
  check_val_every_n_epoch: 20

model:
  warmup_steps: 40

# Long variant uses different env name
env:
  name: "SphereClothCouplingLongEnv"

plot:
  animation:
    fps: 10
    skip_frames: 4
    n_images: 3
  
  plot_at_every_n_val_step: 5

logger:
  group: SphereCloth_100
  job_type: ${model.dynamic_name}
  name: scc_${model.dynamic_name}_seed=${seed}
